// Test des opérateurs arithmétiques dans une fonction


struct test{
    int a;

};

int calcul(void) {
    int x;
    x = 5 + 3;
    x = x - 2;
    x = x * 4;
    x = x / 2;
    x = x % 3;
    return x;
}

int main(void) {
    return calcul();
}
